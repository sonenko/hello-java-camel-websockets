# Hello World for Java + Apache Camel + WebSockets

#### Compile & Run & Play

execute following command from project root

```
./run.sh
```

And then navigate your browser to `http://localhost:8081` and play with it :)
